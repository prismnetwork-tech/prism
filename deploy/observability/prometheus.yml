global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/prism-alerts.yml

scrape_configs:
  - job_name: prism-operations
    static_configs:
      - targets: ["operations-monitor:9091"]
